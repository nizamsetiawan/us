import 'package:get/get.dart';

class FormPerizinanController extends GetxController {
  // Tambahkan logika controller di sini

  Future<void> uploadDataToApi(String title, String filePath) async {
    // Implementasikan logika pengiriman data ke API di sini
    try {} catch (error) {
      // Tangani error jika terjadi
      print('Error: $error');
    }
  }
}
